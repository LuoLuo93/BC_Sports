# BC体育后台管理系统 — 修复清单

> 更新时间：2026-09-08（**三批 31 项 P0/P1 清零 + P2 小修包 ~25 项完成，已提交并推送 main 触发 CICD**）。
> 所有条目均经过两轮独立代码复核 + 修复后 diff 逐文件 review；最终验证：64 个 mapper XML 良构、
> 语句 id 与接口方法一一对应、`mvn compile` 与 `vite build` 全部通过。
> 配套 SQL（staging_tables_full_sync 影子表 8 张 / ODS_SALES_MAIN 索引 / agent_print_permission 打印权限
> / VX_CustomerTag 防重索引）均已在生产库执行。
> 工作量：小 = 半小时内；中 = 半天；大 = 一天以上。
>
> 剩余待办：P2 中等项（F31-F34 Shiro 2.x 升级/授权缓存/RedisSession/MD5限期、F47 LISTAGG优化、F51 list-all远程搜索、F54 尺码组残组、F62 RetryUtil、F63 LzCustomer下沉重构、F70 导入日志composable抽取）与重构 R01 后续/R02/R03；F01 按用户决定跳过。

---

## 已完成（第一批，2026-09-08）

| 编号 | 内容 | 备注 |
|------|------|------|
| F03 ✅ | **P0 收尾：5 处"先删后拉"全部改为影子表原子切换**——企微客户详情/成员详情/群详情走 STG 影子表(拉取写STG→成功后单事务"主表清空+回填")，群列表与企微行为统计改"先拉完再单事务删+插"；失败率>10%或超时/中断时放弃切换保留旧数据并标记任务失败。涉及 8 张主表、7 个 Mapper、4 个任务 | **影子表 DDL 已在两个库执行完毕**；主表结构变更后需 DROP STG 重建 |
| F08 ✅ | @Async 专用线程池 `taskExecutor`(2/8/队列1000/CallerRuns/优雅停机)，@Async 不再回退 SimpleAsyncTaskExecutor 每日志一线程 | TaskThreadPoolConfig |
| F17 ✅ | 实体渠道 syncStoreBrands/batchSave 取号改"循环外预取 max(id)+1 循环内自增"，新增行不再每行一次 MAX(id) 正则全表扫；查询失败回退 UUID | EntityChannelServiceImpl |
| F18 ✅ | 打印任务创建 N+1 消除：模板匹配一次预载分组(排序语义不变)；任务改 batchInsert 每 100 一批(新增 PrintTaskMapper.xml，绕过 MP 主键填充需先 setId) | PrintTaskService、BrandTemplateMatchService.listActiveAll |
| F11 ✅ | PrintController/AgentController 用户侧接口全部补权限：下发/取消/补打=`agent:print:dispatch`(独立按钮权限)，查询=`sticker:agent:query`；pull/result 保持 API-Key 保护不动 | **需在主库执行 `sql/agent_print_permission.sql` 授权按钮给超管，否则现有用户下发操作会 403** |
| F21 ✅ | 会员同步 runPhase 加 Semaphore(5) 并发闸门，不再瞬间塞满共享队列 | NxcrmVipSyncTask |
| F24 ✅ | IHR 批量 10→65 行/批(最宽 employees 表 26 列×65=1690<2100)，全量同步提速约 6 倍 | IhrEmployeeTask |
| F23 ✅ | 企微行为统计改批量接口(userid 逗号串≤100/次)，调用量降百倍；缺席成员单人回退并保留 60111 手机号救援 | QywxApiClient.getUserBehaviorDataBatch + 任务重构 |

## 第三批 — P1 剩余（2026-09-08 完成）

| 编号 | 内容 | 备注 |
|------|------|------|
| F09 ✅ | **5 个导入服务 OPCPackage InputStream OOM 全部修复**：抽公共工具 `ExcelSaxUtils.readAllSheets`（tempFile+File 方式打开，SAX 流式），6 个导入服务（含已修的 OdsSalesMain）统一走一个入口——这是 **R01 导入骨架公共化的第一步**，readAllSheets 六连拷贝已消除 | 后续主表加列/调读取策略只改一处 |
| F12 ✅ | 打印单 IDOR 收口：详情/编辑/提交/删除加属主校验（持 `sticker:print:all` 的管理员可操作所有，普通用户仅本人）；viewAll 参数需 `sticker:print:all` 权限才生效（权限库里已有 STICKER_PRINT_ALL 按钮，前端显隐同权限，无需新 SQL）；审核 review 不校验属主（语义即处理他人单据） | StickerPrintService |
| F13 ✅ | `/images/**` 上传目录 anon→spaAuth（登录后可看，同源 img 自动带 Cookie）；SpaAuthFilter 扩展名白名单显式排除 /images/（堵住"任意 .png 结尾 URL 绕过认证"）；顺带修复 F37（/api/* 未登录返回 200→401） | ShiroConfig、SpaAuthFilter |
| F14 ✅ | Knife4j 文档四路径（doc.html/webjars/v2/api-docs/swagger-resources）anon→spaAuth：登录后仍可查看，不再匿名暴露接口结构 | ShiroConfig |
| F15 ✅ | 登录防爆破两处关键 catch 升级 error 级日志（Redis 故障可见）；`management.health.redis.enabled` false→true（health 如实反映 Redis 故障） | AuthCacheService、application.yml |
| F19 ✅ | 会员标签推送全表载入改 **keyset 分页流式**（nasOuid 游标 5000 行/页 + 跨页会员粘合 + 组合桶满 1000 即发 Phaser 动态等待），内存从"全量会员×标签明细"降为"单页+桶" | NxcrmMemberTagPushTask |
| F20 ✅ | 订单同步：offset 深分页改 keyset（新增 selectOrdersAfter）；批次不再攒全量 Runnable 改即时提交（Phaser）；等待加 60 分钟总超时，超时/中断抛异常任务记失败 | NxcrmOrderSyncTask、NanXOrderMapper |
| F22 ✅ | NxCrmApiClient 9 处请求统一 `setRequestTimeOut(60s)`——**SDK BaseRequest 原生支持**（反编译 ecrp-open-sdk-11.7.0 确认 HttpClientUtil 默认仅 5s 连接超时、请求级读超时需显式设置） | NxCrmApiClient.withTimeout |
| F27 ✅ | 前端 401 单飞标志（一次提示+一次跳转）+ 所有错误弹窗加 `grouping:true` 并发合并 | request.js |
| F29 ✅ | keep-alive 修复：删除手工 pathToName 映射表（已漂移），改 `router.resolve(t.path).name`（路由 name 与组件 name 对齐已验证）——dw-sales/sticker/data/nxcrm 等全部 tab 恢复缓存 | MainLayout.vue |
| F30 ✅ | 删除 three 依赖+3 个 3D 组件+manualChunk 死配置（node_modules 减 112 包）；dashboard 加"演示数据"徽标（KPI/趋势仍是写死的 mock 值，接口未接通前明示）；chunkSizeWarningLimit 1000→500 | package.json、vite.config.js、dashboard |

> 第三批验证：`mvn compile` exit 0、`vite build` exit 0、package.json JSON 合法性校验通过。
> **注意**：F13 上线后未登录直开图片 URL 将 401/302（预期行为）；F14 文档页需先登录。

| 编号 | 内容 | 备注 |
|------|------|------|
| F02 ✅ | Agent API 改 fail-closed（未配密钥拒绝 + `MessageDigest.isEqual` 常量时间比较） | 已确认生产库已配 agent.api-key，无需额外操作 |
| F04 ✅ | 三处 @Transactional 自调用改 TransactionTemplate，deleteAll+insert 恢复原子 | QywxDepartmentTask / QywxFollowUserTask / QywxDepartmentMemberTask |
| F05 ✅ | 解绑任务 while(true) 加 MAX_ROUNDS=200 熔断 | NxcrmUnbindTask |
| F06 ✅ | 云盯 API 重试耗尽改抛异常；YdSyncAllTask 客流/天气子任务隔离 | 失败不再伪装成功，一个失败不连累另一个 |
| F07 ✅ | 4 个接口加 `saveParams=false` 阻止明文密码入日志 | 重置密码 + 新增/更新用户 + 更新个人信息（UserDTO 也含 password） |
| F10 ✅ | ODS_SALES_MAIN 查询索引改幂等 PL/SQL 块 | **待 DBA 在 BI_DW 库执行 dw_sales_main_menu.sql 第 2 部分** |
| F25 ✅ | 删 Element Plus 全量注册，改 el-config-provider | element-plus 产物 925KB → 600KB（-35%） |
| F26 ✅ | CustomerTag.vue 轮询补 onUnmounted 清理 | — |
| F28 ✅ | 删 index.html + dashboard 两处 Google Fonts 外链 | — |
| F01 ⏸ | 明文口令/密钥外置 + 轮换 | **按用户决定暂不执行**（口令无法轮换；将来可做"外置环境变量 + 最小权限账号 + 来源IP限制"的替代方案） |

---

## ~~第二批建议~~（已全部完成，见上方"已完成"表：F03/F08/F17/F18/F11/F21/F24/F23）
| ~~F24~~ ✅ | ~~IHR 员工同步批量=10 行/批~~ 已完成(10→65) | — | — | — |
| F23 ✅(批量接口部分待做) | 企微行为统计逐人串行 HTTP——数据安全部分已完成(先拉完再同事务删+插)，剩余性能优化 | QywxContactCustomerStatisticsTask:94 | 改批量调用，调用量降百倍 | 小 |

## ~~第三批（P1 剩余）~~ — 已全部完成（含 F16 ✅ 数仓销售手工改数补 @OperLog 审计，改动明细入 sys_log 与导入日志对称），明细见上方"第三批"完成表

## P2 — 排期池（按类分组，无变化）

### 架构 / 框架

| 编号 | 问题 | 位置 | 修复方案 | 工作量 |
|------|------|------|----------|--------|
| F31 | Shiro 1.13(javax)+手写桥：每请求反射开销、getSession 恒 null | pom:64-76、ShiroJakartaBridge | 升级 Shiro 2.x 删桥 | 大 |
| F32 | Shiro 授权无缓存（同请求最多 3 次重复查库） | UserRealm:49、GlobalControllerAdvice:41 | Realm 配缓存 + 结果复用 | 中 |
| F33 | 会话纯内存，重启全员掉线 | ShiroConfig:64-80 | RedisSessionDAO | 中 |
| F34 | MD5 旧哈希回退无截止期 | BCryptCredentialsMatcher:51 | 强制迁移+限期失效 | 中 |
| F35 | 登录消息可枚举用户名 | UserRealm:104 | 统一"用户名或密码错误" | 小 |
| F36 | 死配置：shiro: 子树 / CorsConfig 前缀 / db-type 位置 / lettuce pool 无 commons-pool2 | application.yml:253-278、CorsConfig:25-38 | 删死配置或补依赖 | 小 |
| F37 | SpaAuthFilter /api/* 未登录返回 200 非 401 | SpaAuthFilter:24-28 | 补 setStatus | 小 |
| F38 | 500MB 上传×200 线程 OOM 面；文案仍写 100MB | application.yml:341、GlobalExceptionHandler:102 | 限并发；改文案 | 小 |
| F39 | logback includeCallerData + 队列满阻塞 | logback-spring.xml:33-35 | 去 callerData；queue≥2048 | 小 |
| F40 | 调度与手动执行共用 10 线程池；runningJobIds 先加后执行 | ScheduleConfig:61-67/:118-126 | 手动走独立池；调整顺序 | 小 |
| F41 | 验证码 java.util.Random | CaptchaUtil:20 | SecureRandom | 小 |
| F42 | pom 清理：fastjson 零引用 / ecrp-sdk 无仓库 / SpringfoxNpeFix 死代码 | pom:175-186 | 删或补 repositories | 中 |
| F43 | 模块锁 JVM 内，多副本无防护 | ScheduleConfig:196/:268 | Redis SETNX | 中 |

### SQL / 持久层

| 编号 | 问题 | 位置 | 修复方案 | 工作量 |
|------|------|------|----------|--------|
| F44 | CONVERT(DATE,列) 索引失效（ihr 列表页翻页全表聚合×2） | ErpEmployeeSyncStatusMapper:38/111 等 | sargable 范围 + 覆盖索引 | 中 |
| F45 | DATEDIFF 每日全表删除 | VxMassMessageMapper:6 | 范围条件+索引 | 小 |
| F46 | 日志用户名前置通配索引白建 | SysLogMapper:14 | 右模糊/等值 | 小 |
| F47 | 伯俊货品 '%LIKE%'×3 + LISTAGG 分页前聚合 | BjerpProductMapper:8-14/:58 | 内层分页取ID外层聚合 | 中 |
| F48 | HK ERP 同键多查 + OR 条件无索引 | HkBasPersonnelMapper:140/149 | 合并查询；复合索引 | 中 |
| F49 | 群聊去重 List.contains O(n²) | QywxGroupChatTask:124 | HashSet | 小 |
| F50 | 游标 do-while 无页数上限（4 处） | QywxCustomerDetailTask:244 等 | maxPages 熔断 | 小 |
| F51 | ERP 客户/店仓 list-all 全量加载 | ErpCustomerController:58、ErpStoreController:88 | 远程搜索+ROWNUM | 中 |
| F52 | VX_CustomerTag 三列探测无索引 | VxCustomerTagMapper:11-24 | 补复合索引脚本 | 小 |
| F53 | 离职同步 SELECT * 拉 88 列 PII | IhrEmployeeDetailMapper:50-54 | 列清单+索引 | 小 |

### 服务健壮性

| 编号 | 问题 | 位置 | 修复方案 | 工作量 |
|------|------|------|----------|--------|
| F54 | 尺码组导入：事务内组级 catch 吞异常提交残组；DOM readAll 无上限 | StickerSizeGroupService:280/:428-490 | 组级 REQUIRES_NEW；补上限 | 中 |
| F55 | 首加导入 flushBatch 无 catch，账实不符 | BasFirstAddServiceImpl:214 | 照抄 GoodsOldNew 的 catch | 小 |
| F56 | 订单号 Math.random 撞号 | StickerPrintService:289 | 序列或唯一索引+重试 | 小 |
| F57 | cancel 全量拉 CLOB 实体逐条 update | PrintTaskService:647 | 一条 UPDATE 完成 | 小 |
| F58 | 导入统计按 errors.contains("冲突") 反推失真 | EntityChannelServiceImpl:734 | 独立计数器 | 小 |
| F59 | copyPage 漏拷 hasNext/hasPrevious | BeanCopyUtils:50-62 | 补字段拷贝 | 小 |
| F60 | IHR 手动同步吞异常表现成功 | IhrEmployeeTask:680 | rethrow 或失败统计 | 小 |
| F61 | WechatChannel HttpResponse 未关闭 | WechatChannel:131 | try-with-resources | 小 |
| F62 | RetryUtil 按消息文本判可重试 | RetryUtil:128 | 按异常类型 | 中 |
| F63 | LzCustomer 导入写在 Controller、无校验/审计 | LzCustomerController:130 | 下沉 Service 复用骨架 | 中 |

### 前端（P2）

| 编号 | 问题 | 位置 | 修复方案 | 工作量 |
|------|------|------|----------|--------|
| F64 | 会话轮询不感知页面隐藏、无防重入 | useSessionCheck:30 | visibilitychange + inFlight | 小 |
| F65 | AgentMonitor 轮询不感知隐藏 | AgentMonitor:671 | 同上 | 小 |
| F66 | useSyncAction 轮询无超时上限 | useSyncAction:16 | maxPolls | 小 |
| F67 | usePageQuery 无 catch；弹窗分页无竞态保护 | usePageQuery:13、FollowUserList:201 | 补 catch；复用 usePageQuery | 小 |
| F68 | echarts 未注册 Gauge 却注册了没人用的 DataZoom | utils/echarts.js:9-21 | 补 Gauge 删 DataZoom | 小 |
| F69 | JsBarcode 三份+qrcode 共 195KB 孤儿资源 | public/assets/ | 确认后删除 | 小 |
| F70 | 导入日志 40 行两页重复 | DwSalesMain:304、StickerData:395 | 抽 composable | 中 |
| F71 | 环境变量名漂移 VITE_API_BASE(_URL) | .env.*、MainLayout:159 | 统一命名 | 小 |
| F72 | useSyncPolling 死代码 | composables/useSyncPolling.js | 删除 | 小 |

## 重构类（大动作，按需排期）

| 编号 | 内容 | 说明 | 工作量 |
|------|------|------|--------|
| R01 | 导入骨架抽公共类 | 六服务逐字拷贝是"修一处漏五处"根因（F09 即例证） | 大 |
| R02 | API 客户端抽公共骨架 | 五套 client 的 token 管理/重试模板互不复用 | 大 |
| R03 | selectMaxId/selectNextId 五处重复 | 统一 ID 生成策略 | 中 |

---

## 执行顺序（当前状态）

1. **第一批 ✅**：F02/F04/F05/F06/F07/F10/F25/F26/F28（F10 索引与 F03 影子表 SQL 已在库执行）
2. **第二批 ✅**：F03/F08/F17/F18/F11/F21/F24/F23。`agent_print_permission.sql` **已在主库执行完毕**
3. **第三批 ✅（2026-09-08）**：F09(含 R01 第一步)/F12/F13(含F37)/F14/F15/F16/F19/F20/F22/F27/F29/F30
4. **P2 小修包 ✅（2026-09-08）**：F35(确认无需改)/F40(手动执行独立池+拒绝回滚)/F41(SecureRandom)/F36(shiro死子树删除+Cors前缀修正+db-type归位+commons-pool2)/F38(文案100→500MB)/F39(logback queue2048+去callerData)/F44(sargable改造6处+NOT EXISTS范围)/F45(DATEDIFF→范围)/F46(右模糊)/F49(已随F03修复)/F50(游标熔断4处)/F52(VX_CustomerTag索引脚本)/F53(selectRecentLeaved 7列+sargable)/F55(flushBatch加catch)/F57(cancel改单条UPDATE)/F58(conflict计数器)/F59(copyPage补hasPrevious/hasNext)/F60(rethrow)/F61(try-with-resources)/F64(visibility+inFlight)/F65(visibility暂停)/F66(轮询MAX_POLLS超时)/F67(usePageQuery catch)/F68(GaugeChart注册)/F69(删JsBarcode/qrcode 196KB)/F71(删死变量apiBase)/F72(删useSyncPolling死代码)/pom(删SpringfoxNpeFix+redis.version)
5. **剩余**：P2 中等项（F31-F34/F47/F51/F54/F62/F63/F70）与重构 R01 后续/R02/R03；F01 按用户决定跳过

> 上线注意：① 未登录直开 /images/** 图片 URL 将 401/跳登录；② doc.html 接口文档需先登录；③ 打印单操作受属主限制；④ 数仓销售编辑记操作日志；⑤ IHR 员工详情/客户详情/群详情同步改为影子表原子切换，失败保留旧数据。
