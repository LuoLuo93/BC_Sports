-- ============================================================
-- 字段映射改为全局单套：去掉 template_id 维度
-- 所有打印模板字段名一致，不再按模板分组，保留现有 17 条映射数据
-- 部署顺序：先执行本脚本 → 再部署后端(实体已去掉 templateId)→ 再部署前端
-- ============================================================

-- 去掉 template_id 列（Oracle DROP COLUMN 保留其余所有行的数据，17 条映射原样保留）
ALTER TABLE PRINT_FIELD_MAPPING DROP COLUMN TEMPLATE_ID;

COMMIT;
