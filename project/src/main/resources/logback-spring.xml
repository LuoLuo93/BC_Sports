<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 定义日志文件存储路径 -->
    <property name="LOG_PATH" value="./logs"/>
    <property name="LOG_FILE" value="admin"/>
    
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- 异步日志：includeCallerData 每条日志做一次栈快照，开销大且当前无 %class/%method 输出，移除；
         queueSize 512 满时直接阻塞业务线程，提到 2048；discardingThreshold 256 = 队列吃紧时丢
         INFO 级保住 WARN/ERROR 且不再阻塞 -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>2048</queueSize>
        <discardingThreshold>256</discardingThreshold>
    </appender>
    
    <!-- 开发环境 -->
    <springProfile name="dev">
        <logger name="com.bcsport.admin" level="DEBUG"/>
        <logger name="com.bcsport.admin.ihrmapper" level="WARN"/>
        <logger name="com.bcsport.admin.qywxmapper" level="WARN"/>
        <logger name="org.springframework" level="INFO"/>
        <logger name="org.apache.shiro" level="DEBUG"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

    <!-- 生产环境 -->
    <springProfile name="prod">
        <logger name="com.bcsport.admin" level="INFO"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.apache.shiro" level="WARN"/>
        <root level="INFO">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

    <!-- 测试环境 -->
    <springProfile name="test">
        <logger name="com.bcsport.admin" level="DEBUG"/>
        <logger name="com.bcsport.admin.ihrmapper" level="WARN"/>
        <logger name="com.bcsport.admin.qywxmapper" level="WARN"/>
        <logger name="org.springframework" level="INFO"/>
        <logger name="org.apache.shiro" level="DEBUG"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>
</configuration>