<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsport.admin.ihrmapper.IhrDepartmentMapper">
    <delete id="deleteAll">DELETE FROM department</delete>
    <insert id="insertBatch">
        INSERT INTO department(id, department_id, name, parent_id, type, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.departmentId}, #{item.name}, #{item.parentId}, #{item.type}, GETDATE(), GETDATE())
        </foreach>
    </insert>
</mapper>
