<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsport.admin.mapper.nxcrm.NxcrmTagTaskDetailMapper">

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO NXCRM_TAG_TASK_DETAIL
            (ID, TASK_ID, MEMBER_ID, TAG_DATA_JSON, STATUS, CREATE_TIME, UPDATE_TIME)
        <foreach collection="list" item="item" separator="UNION ALL">
            SELECT
                #{item.id},
                #{item.taskId},
                #{item.memberId},
                #{item.tagDataJson},
                #{item.status},
                SYSTIMESTAMP,
                SYSTIMESTAMP
            FROM DUAL
        </foreach>
    </insert>

</mapper>
