<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsport.admin.qywxmapper.QywxAccessTokenMapper">
    <delete id="deleteAll">DELETE FROM VX_AccessToken</delete>
    <insert id="insertBatch">
        INSERT INTO VX_AccessToken(id, access_token, createTime, updateTime)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.accessToken}, GETDATE(), GETDATE())
        </foreach>
    </insert>
    <select id="queryLatestToken" resultType="java.lang.String">
        SELECT TOP 1 access_token
        FROM VX_AccessToken
        ORDER BY createTime DESC
    </select>
</mapper>
