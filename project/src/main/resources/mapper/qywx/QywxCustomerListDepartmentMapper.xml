<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsport.admin.qywxmapper.QywxCustomerListDepartmentMapper">
    <delete id="deleteAll">TRUNCATE TABLE VX_CustomerList_department</delete>
    <insert id="insertBatch">
        INSERT INTO VX_CustomerList_department(id, userid, department, createTime)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.userid}, #{item.department}, GETDATE())
        </foreach>
    </insert>

    <!-- 影子表三件套: 清空STG / 写STG / 切换(与主表deleteAll同一事务调用, 失败回滚保住旧数据) -->
    <update id="clearStg">
        TRUNCATE TABLE VX_CustomerList_department_STG
    </update>

    <insert id="insertBatchStg">
        INSERT INTO VX_CustomerList_department_STG(id, userid, department, createTime)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.userid}, #{item.department}, GETDATE())
        </foreach>
    </insert>

    <update id="copyFromStg">
        INSERT INTO VX_CustomerList_department(id, userid, department, createTime)
        SELECT id, userid, department, createTime
        FROM VX_CustomerList_department_STG
    </update>
</mapper>
