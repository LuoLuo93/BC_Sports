<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsport.admin.qywxmapper.QywxDepartmentMapper">
    <delete id="deleteAll">DELETE FROM VX_DepartmentList</delete>
    <insert id="insertBatch">
        INSERT INTO VX_DepartmentList(id, departId, name, name_en, parentId, parentOrder, createTime, updateTime)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.departId}, #{item.name}, #{item.nameEn}, #{item.parentId}, #{item.parentOrder}, GETDATE(), GETDATE())
        </foreach>
    </insert>
    <select id="selectAll" resultType="com.bcsport.admin.entity.qywx.QywxDepartment">
        SELECT * FROM VX_DepartmentList
    </select>
</mapper>
