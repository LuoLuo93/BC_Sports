<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsport.admin.qywxmapper.VxCustomerBaseMapper">

    <delete id="deleteAll">
        DELETE FROM VX_CustomerBase
    </delete>

    <insert id="insertBatch">
        INSERT INTO VX_CustomerBase (chatId, status, createTime)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.chatId}, #{item.status}, GETDATE())
        </foreach>
    </insert>

    <select id="selectAllChatIds" resultType="java.lang.String">
        SELECT chatId FROM VX_CustomerBase
    </select>

</mapper>
