<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsport.admin.qywxmapper.VxCustomerlistdetailsExternalContactMapper">

    <delete id="deleteAll">
        TRUNCATE TABLE VX_CustomerListDetails_external_contact
    </delete>

    <insert id="insertBatch">
        INSERT INTO VX_CustomerListDetails_external_contact (externalUserid, name, type, gender, unionid, corp_name, createTime)
        VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (#{item.externalUserid}, #{item.name}, #{item.type}, #{item.gender}, #{item.unionid}, #{item.corpName}, GETDATE())
        </foreach>
    </insert>

    <!-- 影子表三件套: 清空STG / 写STG / 切换(与主表deleteAll同一事务调用, 失败回滚保住旧数据) -->
    <update id="clearStg">
        TRUNCATE TABLE VX_CustomerListDetails_external_contact_STG
    </update>

    <insert id="insertBatchStg">
        INSERT INTO VX_CustomerListDetails_external_contact_STG (externalUserid, name, type, gender, unionid, corp_name, createTime)
        VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (#{item.externalUserid}, #{item.name}, #{item.type}, #{item.gender}, #{item.unionid}, #{item.corpName}, GETDATE())
        </foreach>
    </insert>

    <update id="copyFromStg">
        INSERT INTO VX_CustomerListDetails_external_contact (externalUserid, name, type, gender, unionid, corp_name, createTime)
        SELECT externalUserid, name, type, gender, unionid, corp_name, createTime
        FROM VX_CustomerListDetails_external_contact_STG
    </update>

</mapper>
